using UnityEngine;

//bool (불) : 불형, 논리자료형, 참(Ture),거짓(False)값 저장
public class BooleanDemo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //[1] bool 변수 선언하고 초기화
        bool bin = true;
        Debug.Log("bln: "+ bin);

        bool isOut = false;
        Debug.Log("isOut: "+ isOut);


    }
}
